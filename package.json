{
  "name": "parking",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier  \"{apps,libs}/**/*.{ts,tsx,js,json}\" --ignore-path .gitignore",
    "format:check": "pnpm format --check",
    "format:write": "pnpm format --write",
    "tsc": "pnpm --parallel -r run tsc",
    "lint": "pnpm --parallel -r run lint",
    "build": "pnpm --parallel -r run build",
    "validate": "pnpm format:write && pnpm tsc && pnpm lint && pnpm build",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ancss/parking.git"
  },
  "keywords": [],
  "author": "Ancss",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Ancss/parking/issues"
  },
  "homepage": "https://github.com/Ancss/parking#readme",
  "devDependencies": {
    "@types/unist": "^3.0.2",
    "husky": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.4.5"
  }
}
